streamlit>=1.32.0
python-dotenv>=1.0.0
pandas>=2.2.0
numpy<2.0.0
joblib>=1.3.0
scikit-learn>=1.4.0
yfinance>=0.2.37
plotly>=5.19.0
# tensorflow removed — replaced by Chronos (PyTorch-based)
ta>=0.11.0
scipy>=1.12.0
matplotlib>=3.8.0

# Layer 1 — Amazon Chronos foundation model (generative time-series)
chronos-forecasting>=1.3.0
torch>=2.1.0

# Layer 2 — HMM regime detection
hmmlearn>=0.3.0

# Layer 3 — Conformal prediction intervals
# (uses numpy/scipy only — no extra package needed)

# Layer 4 — LLM coherence scoring via Claude Haiku
anthropic>=0.30.0
